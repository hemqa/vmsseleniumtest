package com.vsm.casestudy;

import org.openqa.selenium.*;

public class loginpage extends mainTest {
	
	public void applicationlogin (String username,String password) {
		 //driver.findElement(By.id("UserName")).sendKeys(username);
		 //driver.findElement(By.id("Passwod")).sendKeys(password);
		 //driver.findElement(By.id("btn-login")).click();
		 //System.out.println("Actual Title:"+driver.getTitle().contains("Director"))
		 System.out.println("Successfully login to application");
	  }
}
